<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="GymTracker.View.ExerciseDetailsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:model="clr-namespace:GymTracker.Model"
	xmlns:viewmodel="clr-namespace:GymTracker.ViewModel"
	Title="Details"
	x:DataType="viewmodel:ExerciseDetailsPageViewModel"
	Shell.FlyoutBehavior="Disabled"
	Style="{StaticResource PageStyle}">

	<VerticalStackLayout>
		<HorizontalStackLayout>
			<Label Text="Name:" />
			<Label Text="{Binding Exercise.Name}" />
		</HorizontalStackLayout>
		<HorizontalStackLayout>
			<Label Text="Notes:" />
			<Label Text="{Binding Exercise.Notes}" />
		</HorizontalStackLayout>
		<HorizontalStackLayout>
			<Label Text="Tracking:" />
			<VerticalStackLayout>
				<Label IsVisible="{Binding Exercise.TrackReps}" Text="reps" />
				<Label IsVisible="{Binding Exercise.TrackWeight}" Text="weight" />
				<Label IsVisible="{Binding Exercise.TrackTime}" Text="time" />
			</VerticalStackLayout>
		</HorizontalStackLayout>
	</VerticalStackLayout>
</ContentPage>